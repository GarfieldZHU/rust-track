name: rust-track
on:
  push:
    branches: [ master ]
    paths:
    - 'exercises/**'
  pull_request:
    branches: [ master ]
    paths:
    - 'exercises/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
      
    - uses: actions/checkout@v2
    - name: Test
      run: cargo test --verbose
